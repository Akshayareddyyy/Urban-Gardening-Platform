import type { LucideProps } from 'lucide-react';
import { Sprout as LucideSprout } from 'lucide-react';

export const SproutIcon = (props: LucideProps) => (
  <LucideSprout {...props} />
);

// You can add more custom SVG icons here if needed.
// For example:
// export const MyCustomIcon = (props: React.SVGProps<SVGSVGElement>) => (
//   <svg {...props} viewBox="0 0 24 24" fill="currentColor">
//     {/* SVG path data */}
//   </svg>
// );
